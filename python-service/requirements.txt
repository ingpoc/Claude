# Python Memvid Service Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# Memvid for video-based knowledge storage
memvid>=0.1.3

# Utility dependencies
python-multipart>=0.0.6
requests>=2.31.0

# Optional: For enhanced development
python-dotenv>=1.0.0

# Optional: For AI providers
openai
google-generativeai
anthropic
# Note: OpenRouter can be accessed using the openai library with a custom base URL or via requests which is already included
