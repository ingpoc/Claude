// This component has been removed since we're embedding the entities
// view directly in the page instead of showing a separate overview component.

export default function ProjectOverview() {
  return null;
} 